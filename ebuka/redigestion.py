# lower case ASCII value is between 97 and 122
# upper case ASCII value is between 65 and 90


def case_toggle(words):
    result = ""
    for letter in words:
        ascii_value = ord(words)
        if word
